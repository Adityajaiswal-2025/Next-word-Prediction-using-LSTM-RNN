This project is a Next Word Prediction Model built using LSTM (Long Short-Term Memory) — a type of Recurrent Neural Network (RNN) that learns from sequential data.
The model predicts the next probable word in a sentence based on previously entered words.
